# Demo R3 树状结构（demo-2026-07-21）

> 基于用户 2026-07-21 指令：以 R2（demo-2026-07-20）为参考，落实全局/主管/救生员三端一批改动，UI 覆盖 SPEC 全部功能。
> 三份文件严格共用同一套设计契约（iOS 明暗 token、筛选器、招募卡片、工资大卡片、更表格子系统的 class/CSS 完全一致）。

## 文件清单

```
demo-2026-07-21/
├── 01-supervisor.html   主管端交互（5 分页 + 新页 S9/S6D）
├── 02-lifeguard.html    救生员端交互（含 3 步注册 + 新登录页 L6）
├── 03-admin.html        平台管理员端交互（仅套用全局明暗配色）
└── TREE.md              本文件
```

## 全局（三端一致）

```
＊配色：删除旧「界面个性化」（风格 A/C ＋ 主题色选择器）；改用 iOS 明暗（日/夜）配色，
        CSS 变量 + body.dark 覆盖；顶栏 ☀︎/☾ 切换 + 「我的/设置」页深色开关两处联动。
        顶栏改 iOS 系统栏风格（浅底发丝线，非实心蓝顶）。
＊角标：底部 5 分页右上角微信式红点（沿用 R2，当前分页自动清零）。
＊更表格子：字号 A−/A＋ 缩放，装不下自动出滚动条（全局要求）。
```

## 三端页面层级（按页面 ID）

```
主管端 Supervisor（5 分页：总览 / 更表 / 讯息 / 工资 / 我的）
├── S1 首页（总览）
│   ├── 待處理 → 点入 S5「全部待確認報名」聚合列表（所有池所有待确认，非单人）
│   ├── 今日在崗 → 点入 S9「今日各池在岗总览」（一次列全部泳池＋各池全部在岗人，无层级）
│   ├── 招募情报 → §筛选器（工种/地区级联/显示时薪 ＋ 主管专属「只我发布/排除我」）
│   │            展开即筛选，其下用招募卡片（最大字＝地区级联）列出，含自己发布带管理掣
│   └── ＋发布招募（右下角悬浮 FAB → S7）
├── S2 更表（泳池一览）
│   ├── 顶部：今日 / 明日缺人总数两个数字（点数字筛选缺人泳池，可取消）
│   ├── 發布招募 按钮（同首页 → S7）
│   ├── 筛选：地区 + 缺人筛选，展开显示日历（缺人日红底 / 缺人=0 绿底），
│   │        筛选后先显示「这一天缺人总数」；取消筛选恢复（同 .fbar ✕ 取消）
│   └── 海怡半岛 / 黄埔花园 / 嘉湖山庄 → 点海怡 → S3
├── S3 月更表（Excel 式格子系统）
│   ├── 4 位置列（位置 index 1–4）＋ 每人独一色 pill
│   ├── 拖拽交换（HTML5 drag，格子对格子）
│   ├── 一键排更（填缺人格）＋ 一键整理（竖直对齐到各自主列，算法对齐用户示例）
│   ├── 缺人格点击/长按 → 为该格开招募
│   ├── 假期红底只落在「日期 cell + 星期 cell」
│   └── 月份选择行 ◀ 2025年7月 ▶（对齐 SPEC §5/§6 月更表选模板/选月）
├── S5 全部待確認報名（聚合，要约-接受：主管唯一入表触发者）
├── S6 工资
│   ├── 筛选：按月 / 未结 / 已结（级联）
│   ├── 每人工资大卡片（结清也显金额 + 绿字已結 ✓）→ 整卡点进 S6D
│   └── 确认支付掣样式＝「報名」；已删除原「整人一键确认」ghost 掣
├── S6D 工资详情（单日明细 + 单日确认）［新增］
├── S7 发布招募（由 FAB 进入）
├── S8 我的（账号项 + 深色模式开关；已删风格/主题色选择）
└── S9 今日各池在岗总览［新增］

救生员端 Lifeguard（5 分页）
├── L1 注册（改 3 步 + 顶部进度条 + 鼓励文案）
│   ├── 步骤1 基础账号（用户名/密码/电话号码）
│   ├── 步骤2 上传照片 + 真名（【测试壳】任意相过）
│   └── 步骤3 验证码（显示发去边个电话/WhatsApp + 跳转掣；【测试壳】3 秒自动过）
├── L6 登录［新增］（用户名或手机号 + 密码；忘记密码 / 去注册 / passkey 提示）
├── L2 搵工（§筛选器救生员版 + 招募卡片，最大字＝地区，同主管 S1）
├── L3 我的更表（Excel 视觉复用 + 假期只染日期/星期 cell + A−/A＋ 缩放；单人无拖拽；同池同事/主管 WhatsApp 一键跳转）
├── L4 我的工资（每人卡片外层只显主管名 + 全部应付总额 → 点进 L4 详情看单日明细 + 確認已收）
└── L5 讯息（WhatsApp 式会话列表 → 聊天线程）

管理员端 Admin
├── A1 证件审核（通过 / 驳回 → 联动 A2）
└── A2 用户列表
＊仅套用全局 iOS 明暗配色 + 日/夜切换；结构与交互不变。
```

## 本轮相对上一轮（demo-2026-07-20）的改动

| 端 | 页面 | 改动 |
|----|------|------|
| 全局 | 配色 | **删除风格 A/C ＋ 主题色个性化**，改 **iOS 明暗（日/夜）配色** + 两处联动切换；顶栏改 iOS 系统栏风格 |
| 全局 | 更表 | 格子字号 **A−/A＋ 缩放**，装不下出滚动条；每人一格、格数＝当天所需人数 |
| 主管 | S1 待处理 | 改为入口 → **S5 全部待確認報名聚合**（非单人） |
| 主管 | S1 今日在岗 | 改为入口 → **S9 今日各池在岗总览**（全部池全部人，无层级） |
| 主管 | S1 招募情报 | 静态卡 → **筛选器**（工种/地区级联/显示时薪 + 主管「只我发布/排除我」）+ 招募卡片 |
| 主管 | 招募卡片 | **最大字＝地区级联**，其下才是时间/备注/时薪 |
| 主管 | S2 | 加 **发布招募按钮** + **地区/缺人筛选日历**（红/绿 + 当天缺人总数，可取消） |
| 主管 | S3 | Excel → **格子系统**：位置列 + 每人一色 + 拖拽 + 一键排更 + **一键整理（竖直对齐）** + 缺人格招募 + 月份选择 + 假期只染日期/星期 cell |
| 主管 | S6 | 筛选（月/未结/已结）+ **每人工资大卡片**（结清显金额绿字）→ 点进 **S6D 详情**；删整人一键 ghost；确认掣像報名 |
| 主管 | S8 | 删风格/主题色选择，加深色模式开关 |
| 救生员 | L1 | 注册改 **3 步 + 进度条 + 鼓励文案**；第 3 步显示验证码发去边个号 |
| 救生员 | L6 | **新增登录页** |
| 救生员 | L2 | 换筛选器 + 招募卡片（同主管） |
| 救生员 | L4 | 工资卡片外层只**主管名 + 总额** → 点进详情看明细 + 確認已收 |
| 救生员 | L3 | Excel 视觉复用 + 字号缩放 + 假期只染日期/星期 cell |
| 管理员 | A1/A2 | 仅套用全局 iOS 明暗配色（结构不变） |

## 页面 ID 说明（跨轮固定，旧号永不复用）

- **新增 S9**（今日各池在岗总览）、**S6D**（工资详情子页）、**L6**（登录）。
- S5 语义微调为「全部待确认报名聚合」（仍属待处理报名，非新号）。
- 其余 ID 语义不变。右上角 `.pid` 角标继续显示当前页 ID。

## 已确认端说明

- 管理员端 A 于 R2 定稿存于 `docs/design/confirmed/03-admin.html`（旧配色）。本轮因**全局配色变更**，`demo-2026-07-21/03-admin.html` 已套新 iOS 明暗配色；`confirmed/` 本体**未回写**（等用户确认新配色后再定稿）。

## 同日修订（R3 修订，非新一轮 —— 2026-07-21 用户看完 demo 后原地改这三份文件）

> 用户 6 点反馈，直接在 `demo-2026-07-21/` 三个文件原地修改，未新建文件夹。

| # | 反馈 | 改动 |
|---|------|------|
| 1 | 移除机型选择器，只留 19.5:9 固定比例 | 三端顶栏删除「機型：SE/iPhone 17/17 Pro Max/Android」按钮组 + JS 里 `DEV` 字典与 `setD()`。手机框仍由 `--fw:402px --fh:871px`（≈19.5:9）CSS 变量写死，只是不再暴露切换 UI。顶栏保留一行说明文字（主管端另保留夜间切换按钮）。 |
| 2 | S2 缺人筛选选完自动收起筛选面板 | `showCal()`/`hideCal()` 均调用新增的 `collapseFiltS2()`，选「只看缺人日 / 全部」后自动移除 `.filt.open`，只留 `#s2fbar` 摘要条 + 展开的日历。**地区筛选不收起**（保留面板内高亮 chip 作为「当前生效」提示，方便连续再选缺人；地区没有 `#s2fbar` 摘要条承接状态）。 |
| 3 | S2「招募」入口与 S1 风格统一 | 新增 `.topfab` 按钮 token（实心主色 `--p` + 圆角膠囊 + 白字 + 柔和阴影），与 S1 右下角 `.fab` 共用同一视觉语言。S2 顶栏 `＋招募` 由纯文字 `.act` → `.topfab`；S1 继续用浮动 FAB，位置各自不变。（S6「匯出」仍是 `.act` 文字链，非招募入口，未动。） |
| 4 | （并入第 1 点） | —— |
| 5 | 重新设计低对比 iOS 质感配色 | 三端 `:root`/`body.dark` 统一为一套 token（**逐字相同**，已命令行 diff 验证）：浅色 grouped 灰底 `--bg:#f2f2f7` + 柔黑 `--ink:#1c1c1e`（主管端由刺眼的纯白 `#fff`+纯黑 `#000` 改来）；卡片浮白 `#fff`；深色 `--bg:#000`（iOS 深色正确做法，保留）+ 卡片 `#1c1c1e` 浮起（主管端原本纯黑卡片改来）；发丝线 `#e5e5ea`/`#38383a`、警示 `#ff3b30`/`#ff453a`、成功 `#34c759`/`#30d158`。新增 `--inset`（三端一致）。主管端 card 家族（`.card`/`.filt`/`.reccard`/`.wcard`/`.cal`）由硬边框 `border` 统一改为柔和 `box-shadow`，与救生员/管理员端一致。与 DEMO-RULES §7 参考色值吻合。 |
| 6 | 三端都加天文台信息栏 | 参考 R1（demo-2026-07-19）的 `.wx` 天气栏，用新 token 重新上色（信号 chip 用 iOS 系统色：雷暴 indigo/黄雨 orange/红雨 red/黑雨 gray/天晴 green；条带走 `--card`+`--line` 中性底，随明暗自适应），CSS/HTML/JS 三端**逐字复用**同一套（`cycSig` 循环信号 / `openHKO` 跳天文台 / `tickClock` 更新时间）。落位：主管 **S1 首页顶部**、救生员 **L2 搵工**（登录后默认落地页、最常看首屏、天气与接不接更强相关）、管理员 **A1 证件审核**（主工作页）。 |

**校验**：三文件内联 `<script>` 均通过 `node --check`；`:root{}`/`body.dark{}` 块经 diff 确认三端逐字相同；无残留 `DEV`/`setD`/机型按钮。

## 功能位置对照表（业务功能 → demo 页面，供以后写后端接口用）

> 按 `docs/SPEC.md` 章节顺序整理，每条功能标注当前 demo 里在哪个页面 ID、哪个按钮/组件；**标了"⚠ 缺口"的是 SPEC 里写了但 demo 目前完全没有对应页面**的功能，写接口前要先决定要不要补页面。

| SPEC 章节 | 功能 | Demo 位置 | 备注 |
|---|---|---|---|
| §2 角色 | 主管分级 / 创建子主管账号 / 分配管辖泳池 | — | **⚠ 缺口**：demo 里没有"子主管管理"页面 |
| §2 角色 | 主管兼救生员（自己排进更表） | — | **⚠ 缺口**：SPEC 有概念，demo 未做独立入口（理论上主管账号也会在 S3 里以救生员身份出现） |
| §3.1 | 用户名 / 真实姓名 | L1 注册步骤 1（用户名）、步骤 2（真实姓名） | |
| §3.1 | 对外显示名（各处显示、随时可改） | 显示在 S3/S4/S5/L3/L4/L5 等一切出现人名的地方；**修改入口** | **⚠ 缺口**：demo 没有"个人资料/账号设置"页可以改显示名，注册时只填一次 |
| §3.2 | 注册五要素（用户名/密码/真名/证件OCR/WhatsApp验证） | L1（3 步），OCR 与 WhatsApp 验证均为【测试壳】 | |
| §3.3 | 认证状态机（未认证/已认证/驳回/待复审） | A1（管理员通过/驳回动作）；高亮显示在 S3/S4/S5/L3 等含救生员名字处 | |
| §3.4 | 登录（用户名或手机号+密码） | L6 | |
| §3.4 | 忘记密码（双闸自助） | L6 有一句"忘記密碼？"文字，点击只 toast 占位 | **⚠ 缺口**：完整双闸流程页面还没做 |
| §3.4 | 忘记用户名 / 换手机号 | — | **⚠ 缺口**：demo 完全没有 |
| §3.5 | 保护态（未结工资/未来排班不可自动注销） | — | 纯后端逻辑；A2 用户列表未来可能要显示保护态标记 |
| §4.1 | 招募链接生成 + 分层披露字段勾选 + WhatsApp 文案 | S7 发布招募 | |
| §4.1 | 招募落地页（全平台搜索 + 主管过滤，免登录浏览） | L2 搵工（筛选器 + 招募卡片） | demo 里 L2 是登录后视角，访客态落地页未单独演示 |
| §4.2 | 报名情形 A（直接报名）/ 情形 B（联系以报更） | L2 招募卡片上的按钮 | |
| §4.2 | 泳池详情卡片 + 接受 | S4（主管发卡）/ L5（救生员接受） | |
| §4.2 | 主管最终确认入表 / 抢人冲突隐藏 | S5 全部待确认报名 | |
| §4.3 | 人对人永久会话 | S4（主管）/ L5（救生员） | |
| §5 | 泳池一览 | S2 | |
| §5 | 新建泳池 / 编辑模板（人数/默认价/备注/按星期几更次） | — | **⚠ 缺口**：demo 里 S2 只展示既有泳池，没有"新建/编辑"入口 |
| §5 | 偏好名单（成员 + 最少 N 人） | — | **⚠ 缺口**：只在 S3 一键排班的算法文案里被提及，没有编辑页面 |
| §6.1 | 时段分配 / 拆段接力 | S3 格子系统 | |
| §6.2 | 拖拽约束 / 过去锁死 | S3 | |
| §6.3 | 一键排班 | S3「一鍵排更」 | |
| §6.4 | 排班工具箱：交换 | S3 拖拽交换 | |
| §6.4 | 排班工具箱：请人补空位 | S3 缺人格点击/长按招募 | |
| §6.5 | 取消已确认班次通知主管 | 触发后走站内通知/角标，无独立页面 | |
| §7.1 | 每晚汇总 / 三层时薪解析 | S3（改价）+ S6/S6D 后端逻辑 | |
| §7.2 | 主管工资页 + 未结清余额 + 单日/整人确认 | S6, S6D | |
| §7.2 | 救生员工资页 + 单日确认收款 | L4 | |
| §7.2 | 救生员端同事联系（wa.me） | L3 | |
| §7.2 | Excel 导出 | S6「匯出」按钮 | demo 是 toast 占位，未生成真实文件 |
| §7.3 | 支付确认状态机（7 态 + 撤回） | S6D（主管单日确认）/ L4（救生员确认已收） | demo 只做了单向确认动作，7 态之间的完整流转未全覆盖 |
| §8 | 主管首屏三块（缺人/待处理/在岗） | S1 | |
| §8 | 深色模式切换（日/夜） | S8 设置页 + 全局顶栏 ☀︎/☾ | |
| §8 | 底部导航角标（未读/待办红点） | 三端全局底部 nav | |
| §9 | 站内通知 / Web Push / wa.me 强制跳转 | 分散在各触发点（取消班次、详情卡片接受等）+ 角标提示 | **⚠ 缺口**：没有独立"通知列表/通知中心"页面，只有角标计数 |
| §10 | 证件照片 Private Storage + 后端 Stream 读取 | A1 管理员查看证件图 | demo 是占位图，真实 Stream 读取属后端实现 |

### 已知缺口小结（写接口规划时优先决定要不要补 demo 页面）

1. 个人资料 / 账号设置页（改显示名、看真实姓名、换绑手机号）
2. 忘记密码完整双闸流程页 + 忘记用户名页
3. 新建泳池 / 编辑模板 / 偏好名单编辑页
4. 子主管账号管理页（创建子主管、分配管辖泳池）
5. 通知中心 / 通知列表页（目前只有角标，没有"点开看全部通知"的地方）

以上 5 点如果确认第一期就要做，建议先补对应 demo 页面再进 Spike 周；如果判断为后期迭代功能，也可以先在这里记录、写接口时对应模块先留好扩展点。
