# components/ 共享 UI 组件

跨页面复用的展示型组件放这里。为真正实现时，应与 `docs/design/DEMO-RULES.md` §7 的 demo 类名保持一致，便于「demo → 代码」对照：

- `.filt`：筛选/过滤条
- `.reccard`：招募帖卡片
- `.wcard`：更表/工作卡片

（其余 demo 类名同样在实现对应组件时沿用同名约定。）本单元先留空。
