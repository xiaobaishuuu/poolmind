# 开发日志（每个功能一份，2026-07-19 用户定案）

**规则**：每完成或更新一个功能，就在本目录新增一份 `YYYY-MM-DD-<功能名>.md`，记录：

1. 做了什么（业务语言一句话）
2. **改了哪些模块/文件/函数**（2026-07-21 用户定案，精确到函数级——不只列文件路径，每个改动的文件下面列出改了哪几个函数/方法，新增还是修改；方便以后"这里坏了"能马上定位到具体函数，不用整个文件翻）
3. 对应 SPEC 章节号
4. 遗留事项（含【壳-待补】【临时测试-用完即删】清单）

以后用户说「改 XX 功能」时，先来这里翻对应日志再动手。
